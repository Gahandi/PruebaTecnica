{
    "private": true,
    "scripts": {
        "dev": "echo 'No build process needed with CDN'",
        "build": "echo 'No build process needed with CDN'"
    },
    "devDependencies": {
        "axios": "^1.6.4"
    }
}
